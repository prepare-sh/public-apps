server {
    listen 80;

    location / {
        # "web" is the Compose service name. Docker's embedded DNS returns one
        # address per replica, and nginx round-robins between them.
        proxy_pass http://web:5000;
        proxy_set_header Host $host;
    }
}
