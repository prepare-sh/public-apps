# Copy to .env before starting the stack:  cp .env.example .env
# Used by the db service (env_file:) and by web's DATABASE_URL (variable substitution).
POSTGRES_USER=demo
POSTGRES_PASSWORD=demo_password
POSTGRES_DB=demo
